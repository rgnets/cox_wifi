<%= modal(id: 'securityCodeModal', scrollable: true, size: :xl, aria_labelled_by: 'securityCodeModalTitle', content: { class: ["#{'bg-dark' if @dark_mode}"] }) do %>
  <%= modal_header(close: @dark_mode ? 'light' : true) do %>
    <%= modal_title(id: 'securityCodeModalTitle') do %>
      <%= _('What is the card security code for my credit card?') %>
    <% end # modal_title %>
  <% end #modal_header %>
  <%= modal_body do %>
    <p>
      <%= _('A card security code is a unique security feature of VISA, MasterCard,
      Discover, and American Express credit cards. By including it with your
      purchase, you help protect your account from fraud and you give us one
      more way of being sure your purchase is legitimate.') %>
    </p>
    <p><%= _('Finding your credit card security code:') %></p>
    <%= portal_image_tag('payment/csc_visamc.gif', size: '122x74') %>
    <%= portal_image_tag('payment/csc_amex.gif',   size: '238x74') %>
    <p><strong><%= _('VISA, MC, Discover users') %></strong><br />
    </p>
    <p>
      <%= _('The Card security code is the 3-digit number located on the back of
      your card, usually at the top of the signature strip.') %>
    </p>
    <p><strong><%= _('American Express users') %></strong></p>
    <p><%= _('Look for the 4-digit number printed on the front of your card.') %></p>
    <p>
      <%= _("Depending on which card you have, you'll find this number in small type
      above your credit card's main number on either the left or right side.") %>
    </p>
  <% end #modal_body %>
  <%= modal_footer %>
<% end # modal %>
